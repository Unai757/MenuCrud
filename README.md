##Ejercicio menu crud
En este ejercicio se hace un menu crud con la tabla de ciclistas, usando un programa java la base de datos oracle y usando maven.

Se pueden insertar ciclistas, eliminarlos y actualizarlos como prefiera el usuario ya que hay un menu que deja decidir que hacer.
